# Solar Voyager

A realistic solar-system exploration game that runs in the browser.

Start in low Earth orbit and travel anywhere in the solar system — every planet, moon, dwarf planet, asteroid and comet is there, at real scale, on real orbits, all pulling on your ship at once. Your ship has unlimited propulsion energy and can push arbitrarily close to the speed of light, but every watt-hour is metered at its honest physical price: orbital mechanics and special relativity rule. (A manual 2D launch from Alcântara, Brazil is a planned optional expansion.)

**Play:** deployed on GitHub Pages at `https://tarikdsm.github.io/solar-voyager/` (after milestone M0).

## Highlights

- **Full n-body physics on the ship** — all ~50 bodies attract it simultaneously. Lagrange points, gravity assists and third-body perturbations are real, not scripted.
- **Real scale, real time** — with time warp from 1x to 10,000,000x. What you see is what a real ship would see: true angular sizes, true distances.
- **Special relativity, played straight** — push toward c and watch time dilation split the ship clock from the solar-system clock; energy costs follow the photon-drive bound (P = F·c), so plane changes and near-c cruises cost what physics says they cost.
- **Blender-authored assets** — every model is generated by versioned, reproducible Python scripts.
- **Built by a team of AI agents** (Claude Code, ChatGPT, Grok) working from a shared task queue. See [docs/task-protocol.md](docs/task-protocol.md).

## Tech

Three.js + TypeScript + Vite, Preact HUD, Vitest, GitHub Actions → GitHub Pages. Physics is custom (float64, Dormand–Prince 5(4), analytic Keplerian rails baked from JPL Horizons).

## Documentation

Start with [docs/architecture.md](docs/architecture.md). All design docs live in [docs/](docs/); agent workflow rules in [docs/task-protocol.md](docs/task-protocol.md); the task queue in [tasks/](tasks/).

## Development

```bash
npm install
npm run dev        # local dev server
npm test           # physics + unit tests
npm run build      # production build (base /solar-voyager/)
```

Asset generation requires Blender 4+ (`tools/blender/`), see [docs/asset-pipeline.md](docs/asset-pipeline.md).

## License

MIT. Textures from NASA/USGS (public domain), credited in [docs/asset-pipeline.md](docs/asset-pipeline.md).
