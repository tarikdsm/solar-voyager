# T0090 performance HUD — implementation plan

1. Add red unit tests for the sampled perf-panel store: 4 Hz cadence, one-second
   FPS, 1% low, sparkline chronology/clamping, renderer detail, and stable
   per-frame fast-path signals.
2. Implement the signal store and its self-measured amortized cost without adding
   work to the telemetry ring's allocation-free frame append.
3. Add the compact/expanded `PerfPanel` component, click and `F3` interaction,
   semantic metric anchors, and reserve `F3` in input settings.
4. Wire the store into `main.ts` after telemetry frame completion with the
   current resolution, scale, tier, governor state, and JS heap value.
5. Add contained desktop/mobile styling and adjust the existing top-left panel
   offsets to prevent collisions.
6. Add a lightweight Playwright regression for independent FPS comparison,
   self-cost, expansion, F3, SVG budget line, responsive layout, and browser
   errors; require the panel anchor in the production smoke and add the focused
   regression to the existing CI browser matrix.
7. Run focused red/green tests, lint, typecheck, formatting, all unit/tool/browser
   regressions, build, budgets, task schema, and a real production playtest before
   independent review.
