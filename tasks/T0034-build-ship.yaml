id: T0034
title: Ship model (build_ship script)
status: REVIEW
agent: chatgpt
branch: task/T0034-build-ship
depends_on: [T0030]
milestone: M3
spec: |
  tools/blender/build_ship.py per docs/asset-pipeline.md: original design,
  ~30k tris, PBR materials (albedo/metal-rough/normal/emissive engine glow),
  separate engine-nozzle node for the renderer's plume attachment. Iterate
  via blender-mcp if desired, but back-port everything into the script.
  Needed by M3 (the ship is on screen from the first playable).
acceptance:
  - Ship glb + ktx2 committed, manifest within the 8 MB ship budget
  - Nozzle node named per rendering-spec so the plume can attach
  - Re-running the script reproduces the artifact
handoff_notes: |
  Review evidence (2026-07-17): build_ship.py deterministically recreates the
  approved 26.12 m design with an open engine_nozzle and four project-authored
  1024x512 PBR maps. Two clean Blender 5.1 runs produced matching SHA-256 for
  ship.glb, all maps, and SOURCES.md; the source asset validates at 5,538 tris.
  Focused ingest emitted Draco + four external KTX2 maps totaling 135,605 bytes,
  with exact named-material slots and engine_nozzle preserved. Full ingest
  regenerated public/assets. Verification: 376 JS tests passed (3 skipped), 50
  Python tests passed, Blender smoke passed twice, lint/typecheck/Prettier/build,
  budgets, and task schema passed. Browser smoke rendered one 1920x1080 canvas,
  accepted the 5x warp interaction, and recorded zero console errors.
