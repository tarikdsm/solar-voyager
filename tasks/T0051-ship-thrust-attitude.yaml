id: T0051
title: Ship state, thrust, attitude modes
status: DONE
agent: chatgpt
branch: task/T0051-ship-thrust-attitude
depends_on: [T0050]
milestone: M3
spec: |
  shipState.ts: r, celerity u, attitude quaternion, throttle as fraction of
  max PROPER acceleration alpha (physics-spec section 3 / ADR-007); manual
  rotation + hold modes (pro/retro/normal/anti/radial/target). Thrust enters
  the relativistic derivative function (T0017); drive power P = F*c exposed
  for the ledger and HUD.
acceptance:
  - Prograde hold tracks velocity direction through an orbit (test)
  - Coordinate acceleration falls as 1/gamma^3 parallel to motion (test at gamma=2)
  - Thrust changes trigger predictor invalidation event
handoff_notes: |
  ADR-025 and physics-spec section 3 now define [x,y,z,w] attitude, local +X
  thrust axis, 1 g default proper acceleration, orbital hold frames, exact
  constant-rate manual rotation, photon-drive outputs, and invalidation events.
  Prograde hold stays tangent through a quarter orbit; the propagated gamma=2
  regression recovers alpha/gamma^3. Changed active thrust commands invalidate
  exactly once. Full local CI passed: format, lint, typecheck, 334 TS tests
  (3 skipped), six browser regressions, 49 Python tests, build, schema, and
  budgets. Active-thrust full-catalog benchmark averaged 0.0760 ms/step, reused
  exactly two snapshots, and retained no heap growth.
  Independent review approved exact implementation head 85cb147 with zero
  Critical, Important, or Minor findings after reproducing 29 focused tests,
  334 full tests, and the active-thrust benchmark. Exact-head CI passed.
