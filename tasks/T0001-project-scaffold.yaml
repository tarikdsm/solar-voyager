id: T0001
title: Project scaffold (Vite + TS + three.js + Preact + tooling)
status: REVIEW
agent: chatgpt
branch: task/T0001-project-scaffold-v2
depends_on: []
milestone: M0
spec: |
  Scaffold per docs/architecture.md: Vite + TypeScript strict + three.js + Preact
  (+ @preact/signals), ESLint with import/no-restricted-paths enforcing
  core <- sim <- game <- render/ui, Prettier, Vitest. vite.config.ts with
  base '/solar-voyager/'. npm scripts: dev, build, test, lint, typecheck.
  Placeholder scene: spinning cube + "Solar Voyager" title. Renderer created
  with powerPreference 'high-performance' from day one (full context policy
  arrives in T0045; do not create the context with defaults).
acceptance:
  - npm run lint/typecheck/test/build all pass
  - Import-direction ESLint rule demonstrably rejects a sim -> render import
  - Placeholder scene renders locally via npm run dev
handoff_notes: |
  Ready for external review on task/T0001-project-scaffold-v2. The preserved
  2D baseline at bec528937b703b41c6e1ed86da4699340d6870ea measured
  median/p75/p99 16.7/16.7/16.8 ms and heap delta +61,148 bytes. Two identical
  final-build benchmark runs at fec3ff3f07dbf6e9fdb3bf6c1b6f711f00da9418
  both measured median/p75/p99 16.7/16.7/16.8 ms (p75 variance 0%); heap deltas
  were -1,007,904 and -1,070,052 bytes. Both used 120 warm-up and 600 sampled
  frames at 1280x720 CSS/backing size, recorded zero console/page errors, and
  released fixed port 4174. Canonical after data and comparison are committed
  in 851d869.

  Fresh delivery gates: lint PASS; strict typecheck PASS; all Vitest tests PASS
  (5 files, 10 tests); focused architecture boundary suite PASS (1 file,
  3 tests); production build PASS; Prettier format check PASS; git diff check
  PASS; built index contains exactly two /solar-voyager/assets/ references.
  Vite reports the expected warning for the 531.30 kB uncompressed JavaScript
  chunk (135.03 kB gzip in Vite output).

  Browser gate at 1280x720: visible accessible Solar Voyager h1 PASS; visible
  canvas PASS; canvas and Preact root sibling relationship PASS; WebGL2 context
  PASS; console errors 0; page errors 0. Screenshot artifact:
  output/playwright/T0001-browser-gate.png (36,756 bytes, intentionally
  untracked). Gate server stopped and port 4174 released.

  Dependency ADR coverage: accepted ADR-005 selects Preact plus
  @preact/signals for the HUD, and accepted ADR-008 selects the three.js
  WebGLRenderer/WebGL2 path and renderer performance policy. Together they
  satisfy the runtime-dependency ADR rule in coding-standards.md and the
  renderer policy in performance-spec.md; no duplicate ADR was added.

  A different agent or the human maintainer must review the full branch and
  verify CI before merge. Only that reviewer may merge and flip T0001 to DONE.
