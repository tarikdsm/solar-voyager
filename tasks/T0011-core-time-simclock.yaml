id: T0011
title: core/time — SimClock, TDB seconds, warp ladder
status: DONE
agent: chatgpt
branch: task/T0011-core-time-simclock
depends_on: [T0001]
milestone: M1
spec: |
  Float64 TDB seconds since J2026 epoch; conversions to/from UTC calendar date
  for display; warp ladder constants (physics-spec 3.2) and clamp state type.
acceptance:
  - Epoch round-trip tests (2026-01-01T00:00:00 TDB <-> t=0)
  - Date formatting stable across timezones (UTC only)
handoff_notes: |
  Added caller-owned float64 SimClock state with allocation-free advance, the
  exact physics-spec.md §3.2 warp ladder, and WarpClampState. Added J2026 TDB
  epoch labeling plus a UTC-only display mapping and deterministic formatter;
  the display mapping is documented as separate from orbital timescale math.
  TDD evidence: focused test failed while time.ts was absent, then passed 6/6.
  Full verification passed: lint, typecheck, 90 tests, build, Prettier, task
  schema, asset budgets, and diff check. Independent review confirmed that the
  UTC functions are a deterministic, display-only civil mapping rather than an
  astronomical TDB-to-UTC conversion; orbital time remains float64 TDB seconds.
  The exact frozen ladder, allocation-free clock advance, Date round-trips, and
  formatting under UTC, Pacific/Honolulu, and Asia/Kolkata were verified. PR CI
  run 29461311433 passed on dc2d1c74e8795721948a04a11eb03b4fe97820eb.
