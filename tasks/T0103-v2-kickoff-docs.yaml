id: T0103
title: Add v2 kickoff ADR, roadmap milestones V2M1-V2M6, doc pointers
status: REVIEW
agent: claude
branch: task/T0103-v2-kickoff-docs
depends_on: [T0102]
milestone: V2M1
spec: |
  Lay the documentation foundation for the v2 release. Record the kickoff decision
  (scope, five pillars, non-goals, WebGL2 reaffirmed with WebGPU still deferred per
  ADR-008, budget-revision policy). Add the v2 roadmap section with the six milestones
  and their exit criteria. Correct the stale entries the 2026-08-14 audit found in the
  canonical module map and point agents at the active v2 spec and plan.
  Spec of record: docs/superpowers/specs/2026-08-14-v2-free-flight-design.md sections 3, 13, 14;
  plan docs/superpowers/plans/2026-08-14-v2-free-flight.md T0103.
acceptance:
  - docs/decisions/ADR-032 added (scope, WebGL2 reaffirm, budget policy)
  - docs/roadmap.md gains a v2 section with V2M1-V2M6 exits copied from the spec
  - docs/architecture.md directory map corrected (removes phantom files noted in the 2026-08-14 audit) and lists planned v2 modules
  - AGENTS.md points to the v2 spec+plan as active work
handoff_notes: |
  Docs-only; zero src/ changes. Controller added three items beyond the four acceptance
  criteria: corrected ADR-033's account of how the v1 manifest id list was produced
  (generated via loadCanonicalTasks, not hand-enumerated), scoped the plan's JSON-Schema
  Global Constraint to runtime/catalog data under data/ rather than CI-process config, and
  added a v2.0-development note to release-notes.md. Review found one Important defect —
  the new planned-modules section claimed no listed file existed while ~10 MOD entries are
  real files — fixed in 209f072 together with two minor attribution errors. Verification:
  format:check, check:tasks (61), check:release, check:dashboard, 833 Vitest passed / 3
  skipped; the reviewer independently re-derived the module map against the real tree.
  Deferred minors: directory-layout lines remain partial by convention (sim/analysis lists
  4 of 8 modules); expansion-hooks rows for Vessel and SystemDefinition still describe hooks
  that do not exist in src/ (pre-existing text; Vessel becomes real in T0104).
