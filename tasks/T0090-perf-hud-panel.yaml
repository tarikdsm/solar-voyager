id: T0090
title: Top-left perf panel (FPS, sparkline, resolution, tier)
status: DONE
agent: chatgpt
branch: task/T0090-perf-hud-panel
depends_on: [T0045, T0054]
milestone: M3
spec: |
  performance-spec section 4: elegant compact row — FPS (1 s avg),
  120-frame frame-time sparkline with 16.6 ms budget line, render
  resolution with scale (1920x1080 @0.85), quality-tier badge; F3 expands
  to full telemetry (1% lows, ms splits, draw calls/triangles, memory,
  GPU name, governor state). 4 Hz updates except the sparkline (ring
  buffer, per frame). Monospace numerals, low-contrast until hover.
acceptance:
  - Panel total cost < 0.2 ms/frame (measured by its own telemetry)
  - Values cross-checked against browser devtools FPS meter
  - Zero allocations per frame in the sparkline path
handoff_notes: >-
  Added the compact/expanded 4 Hz signal-driven perf HUD. Review corrections
  replaced SVG point-string construction with a setup-time Canvas 2D sink,
  added RenderTelemetry's preallocated 256-timestamp true one-second FPS window,
  measured sampled and fast-path panel calls, included the panel in UI/HUD time,
  and ignored repeated F3 keydowns. Focused Chromium measured 60.0 FPS against
  an independent 60.0 FPS meter and 0.012 ms/frame isolated (0.056 while the
  browser matrix ran concurrently); full-game playtest measured 0.018 ms/frame,
  85.0 FPS, 0.30 ms UI/HUD, and no warnings/errors. Responsive 1280/1024/390 px
  layout, production smoke, 492 Vitest tests, 50 Python tests, build, budgets,
  and the complete sequential browser matrix pass. Independent re-review of
  SHA 9db589ed approved with zero Critical, Important, or Minor findings after
  reproducing 13 focused Vitest tests, Chromium perf regression at
  0.027 ms/frame, production smoke, and the green exact-head CI run.
