id: T0059
title: Playwright smoke test in CI
status: REVIEW
agent: chatgpt
branch: task/T0059-playwright-smoke
depends_on: [T0054]
milestone: M3
spec: |
  Page loads, canvas renders (pixel probe), HUD mounts, no console errors;
  wired into ci.yml.
acceptance:
  - Smoke test red on an injected runtime error (fixture), green on main
handoff_notes: >-
  Production-dist smoke starts Vite preview at the GitHub Pages base path, probes
  live WebGL framebuffer pixels and HUD anchors, and rejects console/page errors
  and crashes. Committed immediate and framebuffer-timed init fixtures prove
  both runtime-error paths fail; npm run test:smoke proves both fixture
  rejections and clean-page success. Full local CI matrix passed on 2026-07-17.
